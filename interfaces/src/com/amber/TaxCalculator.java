package com.amber;

public interface TaxCalculator {
    //float would be constant because its in the interface
//    float minimumTax = 100;
    double calculateTax();
}
//ALL INTERFACE METHODS ARE PUBLIC
//don't write public. its redundant